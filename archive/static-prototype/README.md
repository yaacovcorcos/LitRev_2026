# Legacy static prototype

This folder stores the original static HTML/CSS/JS prototype for reference only. The active app lives in `next-app/`, so tools and agents should ignore this directory unless specifically asked to revisit the legacy markup.
