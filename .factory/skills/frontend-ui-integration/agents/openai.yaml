interface:
  display_name: "Frontend UI Integration"
  short_description: "Implement or extend LitRev frontend workflows in next-app/ while following repo-owned frontend doctrine, the frontend specialist, the review loop, and validation gates."
  default_prompt: "Use $frontend-ui-integration to implement or extend this LitRev frontend workflow in next-app/ while following repo-owned frontend doctrine, the frontend specialist, the frontend review loop, and the required validation gates."
