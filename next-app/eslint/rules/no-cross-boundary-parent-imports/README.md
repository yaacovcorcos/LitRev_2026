# litrev/no-cross-boundary-parent-imports

Across `app`, `components`, `contexts`, and `hooks`, LitRev prefers `@/` imports for cross-boundary references. Same-folder relative imports are fine; parent-directory climbs are not.
