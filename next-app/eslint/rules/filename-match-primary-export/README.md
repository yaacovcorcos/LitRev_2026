# litrev/filename-match-primary-export

When a file has one obvious primary export, LitRev prefers the filename to match it. This stays intentionally lighter than Factory's suffix-specific naming rules.
